{
  "lint": {
    "rules": {
      "exclude": [
        "no-explicit-any",
        "no-import-prefix"
      ]
    }
  },
  "fmt": {
    "lineWidth": 100
  },
  "tasks": {
    "test": "deno test _tests/",
    "verif": "deno fmt --check && deno lint && deno check **/index.ts _tests/*.ts && deno test _tests/"
  }
}
