project_id = "alerte-incendie"

[functions.poll-firms]
verify_jwt = false          # protégée par x-admin-key, appelée par pg_cron

[functions.dispatch]
verify_jwt = false          # protégée par x-admin-key ou service role

[functions.load-communes]
verify_jwt = false          # protégée par x-admin-key

[functions.api]
verify_jwt = false          # protégée par jeton d'abonné (x-token)

[functions.poll-lsasaf]
verify_jwt = false          # protégée par x-admin-key, appelée par pg_cron

[functions.probe-lsasaf]
verify_jwt = false          # protégée par x-admin-key, usage diagnostique

[functions.signalement]
verify_jwt = false          # écriture protégée par jeton d'abonné, carte publique

[functions.poll-meteo]
verify_jwt = false          # protégée par x-admin-key, appelée par pg_cron

[functions.poll-adsb]
verify_jwt = false          # protégée par x-admin-key, inactive sans config.adsb

[functions.probe-mtg]
verify_jwt = false          # protégée par x-admin-key, veille mensuelle

[functions.probe-sentinel3]
verify_jwt = false          # protégée par x-admin-key, veille mensuelle STAC
